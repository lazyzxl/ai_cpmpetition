<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.CompetitionMapper">
    
    <insert id="insert" parameterType="com.sky.entity.Competition">
        insert into competition (id, name, title, description, url, date, update_time, photourl, create_time, organizer, location, participant_limit, prize, contact)
        values (#{id}, #{name}, #{title}, #{description}, #{url}, #{date}, #{updateTime}, #{photourl}, #{createTime}, #{organizer}, #{location}, #{participantLimit}, #{prize}, #{contact})
    </insert>
    
    <update id="update" parameterType="com.sky.entity.Competition">
        update competition
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="url != null">url = #{url},</if>
            <if test="date != null">date = #{date},</if>
            <if test="photourl != null">photourl = #{photourl},</if>
            <if test="organizer != null">organizer = #{organizer},</if>
            <if test="location != null">location = #{location},</if>
            <if test="participantLimit != null">participant_limit = #{participantLimit},</if>
            <if test="prize != null">prize = #{prize},</if>
            <if test="contact != null">contact = #{contact},</if>
            update_time = #{updateTime}
        </set>
        where id = #{id}
    </update>
    
    <select id="getByIds" parameterType="java.util.List" resultType="com.sky.entity.Competition">
        select * from competition
        where id in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    
</mapper>
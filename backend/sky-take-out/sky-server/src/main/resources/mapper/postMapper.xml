<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.postMapper">
    <select id="getById" resultType="com.sky.entity.Post">
        select * from post where id = #{postId}
    </select>

    <select id="page" resultType="com.sky.entity.Post">
        select * from post
        <where>
            name like concat('%',#{name},'%')
        </where>
        order by create_time desc
    </select>

    <select id="list" resultType="com.sky.entity.Post">
        select * from post order by create_time desc
    </select>

    <insert id="insert" parameterType="com.sky.entity.Post">
        insert into post (user_id, title, content, tag, create_time, status)
        values (#{userId}, #{title}, #{content}, #{tag}, #{createTime}, #{status})
    </insert>
</mapper>
